# Migration Diff Report

`2026-03-06 02:36:11` — 73 files · 27 errors · 3 warnings · 3 info

---

## Migration Summary

19 composables · 27 errors · 17 warnings · 8 info

---

<a id="themeMixin"></a>

### 🔴 [`themeMixin`](src/mixins/themeMixin.js) → [`useTheme`](src/composables/useTheme.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`App.vue`](src/App.vue): this.$el has no composable equivalent | Use a template ref on the root element instead |
| info | [`App.vue`](src/App.vue): $forceUpdate — rarely needed in Vue 3 | Reactive system usually handles it; review logic |
| warning | [`AppHeader.vue`](src/components/layout/AppHeader.vue): Mixin 'themeMixin' was NOT migrated: it provides lifecycle hooks (created) but no members are directly referenced by the component. | Manually convert lifecycle hooks to the composable, or remove the mixin if unused. |

<a id="modalMixin"></a>

### 🔴 [`modalMixin`](src/mixins/modalMixin.js) → [`useModal`](src/composables/useModal.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`BaseModal.vue`](src/components/common/BaseModal.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| error | [`BaseModal.vue`](src/components/common/BaseModal.vue): this.$refs is not available in composables | Use template refs with ref() instead |

<a id="keyboardShortcutMixin"></a>

### 🔴 [`keyboardShortcutMixin`](src/mixins/keyboardShortcutMixin.js) → [`useKeyboardShortcut`](src/composables/useKeyboardShortcut.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`BaseModal.vue`](src/components/common/BaseModal.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| error | [`BaseModal.vue`](src/components/common/BaseModal.vue): this.$el has no composable equivalent | Use a template ref on the root element instead |

<a id="tableMixin"></a>

### 🔴 [`tableMixin`](src/mixins/tableMixin.js) → [`useTable`](src/composables/useTable.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`DataTable.vue`](src/components/common/DataTable.vue): this.$refs is not available in composables | Use template refs with ref() instead |
| error | [`DataTable.vue`](src/components/common/DataTable.vue): this.$el has no composable equivalent | Use a template ref on the root element instead |
| warning | [`DataTable.vue`](src/components/common/DataTable.vue): Nested mixins [sortMixin('name'), paginationMixin] — transitive members may be missed | Ensure all transitive mixin members are accounted for |

<a id="selectionMixin"></a>

### 🔴 [`selectionMixin`](src/mixins/selectionMixin.js) → [`useSelection`](src/composables/useSelection.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`DataTable.vue`](src/components/common/DataTable.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| info | [`UserList.vue`](src/components/users/UserList.vue): Component overrides mixin member(s): allSelected. These are NOT destructured from the composable. | Verify that the component's own allSelected implementation does not depend on other mixin members. |

<a id="formMixin"></a>

### 🔴 [`formMixin`](src/mixins/formMixin.js) → [`useForm`](src/composables/useForm.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`FileUploader.vue`](src/components/common/FileUploader.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| error | [`FileUploader.vue`](src/components/common/FileUploader.vue): this.$refs is not available in composables | Use template refs with ref() instead |
| warning | [`ProjectForm.vue`](src/components/projects/ProjectForm.vue): 'formData' exists in both setup() return and data() — data() will shadow the setup() value | Remove 'formData' from data() or rename it to avoid collision |
| warning | [`LoginView.vue`](src/views/LoginView.vue): 'isSubmitting' exists in both setup() return and data() — data() will shadow the setup() value | Remove 'isSubmitting' from data() or rename it to avoid collision |
| info | [`LoginView.vue`](src/views/LoginView.vue): Component overrides mixin member(s): isSubmitting. These are NOT destructured from the composable. | Verify that the component's own isSubmitting implementation does not depend on other mixin members. |
| warning | [`SettingsView.vue`](src/views/SettingsView.vue): Mixin 'formMixin' was NOT migrated: it provides lifecycle hooks (beforeMount) but no members are directly referenced by the component. | Manually convert lifecycle hooks to the composable, or remove the mixin if unused. |

<a id="validationMixin"></a>

### 🔴 [`validationMixin`](src/mixins/validationMixin.js) → [`useValidation`](src/composables/useValidation.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`FileUploader.vue`](src/components/common/FileUploader.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| error | [`FileUploader.vue`](src/components/common/FileUploader.vue): 'formData' — external dep, not available in composable scope.
// Accept as composable param: this.formData → formData.value or formData. | Accept 'formData' as a composable parameter and rewrite this.formData |
| warning | [`UserForm.vue`](src/components/users/UserForm.vue): 'rules' exists in both setup() return and data() — data() will shadow the setup() value | Remove 'rules' from data() or rename it to avoid collision |
| info | [`UserForm.vue`](src/components/users/UserForm.vue): Component overrides mixin member(s): rules. These are NOT destructured from the composable. | Verify that the component's own rules implementation does not depend on other mixin members. |
| warning | [`LoginView.vue`](src/views/LoginView.vue): Mixin 'validationMixin' was NOT migrated: no members are referenced by the component. | Remove the mixin if unused, or manually convert if it provides side-effect functionality. |

<a id="filterMixin"></a>

### 🔴 [`filterMixin`](src/mixins/filterMixin.js) → [`useFilter`](src/composables/useFilter.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`FilterPanel.vue`](src/components/common/FilterPanel.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| warning | [`CalendarView.vue`](src/views/CalendarView.vue): Mixin 'filterMixin' was NOT migrated: no members are referenced by the component. | Remove the mixin if unused, or manually convert if it provides side-effect functionality. |

<a id="undoRedoMixin"></a>

### 🔴 [`undoRedoMixin`](src/mixins/undoRedoMixin.js) → [`useUndoRedo`](src/composables/useUndoRedo.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`RichTextEditor.vue`](src/components/common/RichTextEditor.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |

<a id="searchMixin"></a>

### 🔴 [`searchMixin`](src/mixins/searchMixin.js) → [`useSearch`](src/composables/useSearch.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`SearchBar.vue`](src/components/common/SearchBar.vue): this.$refs is not available in composables | Use template refs with ref() instead |
| error | [`SearchBar.vue`](src/components/common/SearchBar.vue): 'items' — external dep, not available in composable scope.
// Accept as composable param: this.items → items.value or items. | Accept 'items' as a composable parameter and rewrite this.items |
| error | [`SearchBar.vue`](src/components/common/SearchBar.vue): '_searchTimeout' — external dep, not available in composable scope.
// Accept as composable param: this._searchTimeout → _searchTimeout.value or _searchTimeout. | Accept '_searchTimeout' as a composable parameter and rewrite this._searchTimeout |

<a id="chartMixin"></a>

### 🔴 [`chartMixin`](src/mixins/chartMixin.js) → [`useChart`](src/composables/useChart.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`ProjectSummaryChart.vue`](src/components/dashboard/ProjectSummaryChart.vue): this.$refs is not available in composables | Use template refs with ref() instead |
| error | [`ProjectSummaryChart.vue`](src/components/dashboard/ProjectSummaryChart.vue): this.$el has no composable equivalent | Use a template ref on the root element instead |
| warning | [`ReportChart.vue`](src/components/reports/ReportChart.vue): 'chartOptions' exists in both setup() return and data() — data() will shadow the setup() value | Remove 'chartOptions' from data() or rename it to avoid collision |
| info | [`ReportChart.vue`](src/components/reports/ReportChart.vue): Component overrides mixin member(s): chartOptions. These are NOT destructured from the composable. | Verify that the component's own chartOptions implementation does not depend on other mixin members. |
| warning | [`DashboardView.vue`](src/views/DashboardView.vue): Mixin 'chartMixin' was NOT migrated: it provides lifecycle hooks (mounted, beforeUnmount) but no members are directly referenced by the component. | Manually convert lifecycle hooks to the composable, or remove the mixin if unused. |

<a id="notificationMixin"></a>

### 🔴 [`notificationMixin`](src/mixins/notificationMixin.js) → [`useNotification`](src/composables/useNotification.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`AppHeader.vue`](src/components/layout/AppHeader.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| warning | [`NotificationItem.vue`](src/components/notifications/NotificationItem.vue): Mixin 'notificationMixin' was NOT migrated: it provides lifecycle hooks (created) but no members are directly referenced by the component. | Manually convert lifecycle hooks to the composable, or remove the mixin if unused. |

<a id="dragDropMixin"></a>

### 🔴 [`dragDropMixin`](src/mixins/dragDropMixin.js) → [`useDragDrop`](src/composables/useDragDrop.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`ProjectBoard.vue`](src/components/projects/ProjectBoard.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| error | [`ProjectBoard.vue`](src/components/projects/ProjectBoard.vue): this.$refs is not available in composables | Use template refs with ref() instead |
| error | [`ProjectBoard.vue`](src/components/projects/ProjectBoard.vue): this.$el has no composable equivalent | Use a template ref on the root element instead |
| warning | [`KanbanColumn.vue`](src/components/tasks/KanbanColumn.vue): Mixin 'dragDropMixin' was NOT migrated: no members are referenced by the component. | Remove the mixin if unused, or manually convert if it provides side-effect functionality. |

<a id="commentMixin"></a>

### 🔴 [`commentMixin`](src/mixins/commentMixin.js) → [`useComment`](src/composables/useComment.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`TaskComments.vue`](src/components/tasks/TaskComments.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| error | [`TaskComments.vue`](src/components/tasks/TaskComments.vue): this.$refs is not available in composables | Use template refs with ref() instead |
| error | [`TaskComments.vue`](src/components/tasks/TaskComments.vue): 'entityId' — external dep, not available in composable scope.
// Accept as composable param: this.entityId → entityId.value or entityId. | Accept 'entityId' as a composable parameter and rewrite this.entityId |
| warning | [`TaskDetailView.vue`](src/views/TaskDetailView.vue): Mixin 'commentMixin' was NOT migrated: it provides lifecycle hooks (mounted) but no members are directly referenced by the component. | Manually convert lifecycle hooks to the composable, or remove the mixin if unused. |

<a id="authMixin"></a>

### 🔴 [`authMixin`](src/mixins/authMixin.js) → [`useAuth`](src/composables/useAuth.js) · (LOW)

| Severity | Issue | Fix |
|---|---|---|
| error | [`LoginView.vue`](src/views/LoginView.vue): this.$emit is not available in composables | Accept an emit function parameter or use defineEmits |
| warning | [`LoginView.vue`](src/views/LoginView.vue): this.$router is not available in composables | Import and use useRouter() from vue-router |

<a id="loadingMixin"></a>

### 🟡 [`loadingMixin`](src/mixins/loadingMixin.js) → [`useLoading`](src/composables/useLoading.js) · (MEDIUM)

| Severity | Issue | Fix |
|---|---|---|
| info | [`EmptyState.vue`](src/components/common/EmptyState.vue): $forceUpdate — rarely needed in Vue 3 | Reactive system usually handles it; review logic |
| warning | [`NotFoundView.vue`](src/views/NotFoundView.vue): Mixin 'loadingMixin' was NOT migrated: no members are referenced by the component. | Remove the mixin if unused, or manually convert if it provides side-effect functionality. |

<a id="permissionMixin"></a>

### 🟡 [`permissionMixin`](src/mixins/permissionMixin.js) → [`usePermission`](src/composables/usePermission.js) · (MEDIUM)

| Severity | Issue | Fix |
|---|---|---|
| warning | [`StatusBadge.vue`](src/components/common/StatusBadge.vue): this.$router is not available in composables | Import and use useRouter() from vue-router |
| info | [`StatusBadge.vue`](src/components/common/StatusBadge.vue): Mixin 'permissionMixin' was NOT migrated: all used members (canDelete) are overridden by the component. | Remove the mixin if the component's overrides are self-contained, or keep it if they depend on mixin internals. |
| warning | [`ProjectDetailView.vue`](src/views/ProjectDetailView.vue): Mixin 'permissionMixin' was NOT migrated: no members are referenced by the component. | Remove the mixin if unused, or manually convert if it provides side-effect functionality. |

<a id="paginationMixin"></a>

### 🟢 [`paginationMixin`](src/mixins/paginationMixin.js) → [`usePagination`](src/composables/usePagination.js) · (HIGH)

| Severity | Issue | Fix |
|---|---|---|
| info | [`RecentActivity.vue`](src/components/dashboard/RecentActivity.vue): Component overrides mixin member(s): hasPrevPage. These are NOT destructured from the composable. | Verify that the component's own hasPrevPage implementation does not depend on other mixin members. |

<a id="exportMixin"></a>

### 🟢 [`exportMixin`](src/mixins/exportMixin.js) → [`useExport`](src/composables/useExport.js) · (HIGH)

No manual changes needed.


## Per-Component Guide

### [`App.vue`](src/App.vue)

- 🔴 [`useTheme`](src/composables/useTheme.js) — 1 error → [See warnings](#themeMixin)

### [`BaseModal.vue`](src/components/common/BaseModal.vue)

- 🔴 [`useModal`](src/composables/useModal.js) — 2 errors → [See warnings](#modalMixin)
- 🔴 [`useKeyboardShortcut`](src/composables/useKeyboardShortcut.js) — 2 errors → [See warnings](#keyboardShortcutMixin)

### [`ConfirmDialog.vue`](src/components/common/ConfirmDialog.vue)

- 🔴 [`useModal`](src/composables/useModal.js) — 2 errors → [See warnings](#modalMixin)

### [`DataTable.vue`](src/components/common/DataTable.vue)

- 🔴 [`useTable`](src/composables/useTable.js) — 2 errors, 1 warning → [See warnings](#tableMixin)
- 🔴 [`useSelection`](src/composables/useSelection.js) — 1 error → [See warnings](#selectionMixin)
- 🟢 [`usePagination`](src/composables/usePagination.js) — No issues

### [`EmptyState.vue`](src/components/common/EmptyState.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`ExportButton.vue`](src/components/common/ExportButton.vue)

- 🟢 [`useExport`](src/composables/useExport.js) — No issues

### [`FileUploader.vue`](src/components/common/FileUploader.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors → [See warnings](#formMixin)
- 🔴 [`useValidation`](src/composables/useValidation.js) — 2 errors → [See warnings](#validationMixin)

### [`FilterPanel.vue`](src/components/common/FilterPanel.vue)

- 🔴 [`useFilter`](src/composables/useFilter.js) — 1 error → [See warnings](#filterMixin)

### [`LoadingSpinner.vue`](src/components/common/LoadingSpinner.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`PaginationBar.vue`](src/components/common/PaginationBar.vue)

- 🟢 [`usePagination`](src/composables/usePagination.js) — No issues

### [`RichTextEditor.vue`](src/components/common/RichTextEditor.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors → [See warnings](#formMixin)
- 🔴 [`useUndoRedo`](src/composables/useUndoRedo.js) — 1 error → [See warnings](#undoRedoMixin)

### [`SearchBar.vue`](src/components/common/SearchBar.vue)

- 🔴 [`useSearch`](src/composables/useSearch.js) — 3 errors → [See warnings](#searchMixin)

### [`StatusBadge.vue`](src/components/common/StatusBadge.vue)

- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`ProjectSummaryChart.vue`](src/components/dashboard/ProjectSummaryChart.vue)

- 🔴 [`useChart`](src/composables/useChart.js) — 2 errors → [See warnings](#chartMixin)

### [`RecentActivity.vue`](src/components/dashboard/RecentActivity.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🟢 [`usePagination`](src/composables/usePagination.js) — No issues

### [`StatsOverview.vue`](src/components/dashboard/StatsOverview.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🔴 [`useChart`](src/composables/useChart.js) — 2 errors → [See warnings](#chartMixin)

### [`TaskDistribution.vue`](src/components/dashboard/TaskDistribution.vue)

- 🔴 [`useChart`](src/composables/useChart.js) — 2 errors → [See warnings](#chartMixin)
- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`UpcomingDeadlines.vue`](src/components/dashboard/UpcomingDeadlines.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`AppBreadcrumb.vue`](src/components/layout/AppBreadcrumb.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`AppFooter.vue`](src/components/layout/AppFooter.vue)

- 🔴 [`useTheme`](src/composables/useTheme.js) — 1 error → [See warnings](#themeMixin)

### [`AppHeader.vue`](src/components/layout/AppHeader.vue)

- ℹ️ **themeMixin** skipped — Mixin 'themeMixin' was NOT migrated: it provides lifecycle hooks (created) but no members are directly referenced by the component.
- 🔴 [`useNotification`](src/composables/useNotification.js) — 1 error → [See warnings](#notificationMixin)

### [`AppLayout.vue`](src/components/layout/AppLayout.vue)

- 🔴 [`useKeyboardShortcut`](src/composables/useKeyboardShortcut.js) — 2 errors → [See warnings](#keyboardShortcutMixin)

### [`AppSidebar.vue`](src/components/layout/AppSidebar.vue)

- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`ThemeSwitcher.vue`](src/components/layout/ThemeSwitcher.vue)

- 🔴 [`useTheme`](src/composables/useTheme.js) — 1 error → [See warnings](#themeMixin)

### [`NotificationBell.vue`](src/components/notifications/NotificationBell.vue)

- 🔴 [`useNotification`](src/composables/useNotification.js) — 1 error → [See warnings](#notificationMixin)

### [`NotificationItem.vue`](src/components/notifications/NotificationItem.vue)

- ℹ️ **notificationMixin** skipped — Mixin 'notificationMixin' was NOT migrated: it provides lifecycle hooks (created) but no members are directly referenced by the component.

### [`NotificationList.vue`](src/components/notifications/NotificationList.vue)

- 🔴 [`useNotification`](src/composables/useNotification.js) — 1 error → [See warnings](#notificationMixin)
- 🟢 [`usePagination`](src/composables/usePagination.js) — No issues

### [`ProjectBoard.vue`](src/components/projects/ProjectBoard.vue)

- 🔴 [`useDragDrop`](src/composables/useDragDrop.js) — 3 errors → [See warnings](#dragDropMixin)
- 🔴 [`useSelection`](src/composables/useSelection.js) — 1 error → [See warnings](#selectionMixin)

### [`ProjectCard.vue`](src/components/projects/ProjectCard.vue)

- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`ProjectForm.vue`](src/components/projects/ProjectForm.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors, 1 warning → [See warnings](#formMixin)
- 🔴 [`useValidation`](src/composables/useValidation.js) — 2 errors → [See warnings](#validationMixin)

### [`ProjectList.vue`](src/components/projects/ProjectList.vue)

- 🔴 [`useFilter`](src/composables/useFilter.js) — 1 error → [See warnings](#filterMixin)
- 🟢 [`usePagination`](src/composables/usePagination.js) — No issues

### [`ProjectMembers.vue`](src/components/projects/ProjectMembers.vue)

- 🔴 [`useSelection`](src/composables/useSelection.js) — 1 error → [See warnings](#selectionMixin)
- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`ProjectSettings.vue`](src/components/projects/ProjectSettings.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors, 1 warning → [See warnings](#formMixin)
- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`ProjectTimeline.vue`](src/components/projects/ProjectTimeline.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🔴 [`useChart`](src/composables/useChart.js) — 2 errors → [See warnings](#chartMixin)

### [`ReportBuilder.vue`](src/components/reports/ReportBuilder.vue)

- 🔴 [`useChart`](src/composables/useChart.js) — 2 errors → [See warnings](#chartMixin)
- 🟢 [`useExport`](src/composables/useExport.js) — No issues
- 🔴 [`useFilter`](src/composables/useFilter.js) — 1 error → [See warnings](#filterMixin)

### [`ReportChart.vue`](src/components/reports/ReportChart.vue)

- 🔴 [`useChart`](src/composables/useChart.js) — 2 errors, 1 warning → [See warnings](#chartMixin)

### [`ReportExport.vue`](src/components/reports/ReportExport.vue)

- 🟢 [`useExport`](src/composables/useExport.js) — No issues

### [`ReportTable.vue`](src/components/reports/ReportTable.vue)

- 🔴 [`useTable`](src/composables/useTable.js) — 2 errors, 1 warning → [See warnings](#tableMixin)
- 🟢 [`useExport`](src/composables/useExport.js) — No issues

### [`KanbanColumn.vue`](src/components/tasks/KanbanColumn.vue)

- ℹ️ **dragDropMixin** skipped — Mixin 'dragDropMixin' was NOT migrated: no members are referenced by the component.
- 🔴 [`useSelection`](src/composables/useSelection.js) — 1 error → [See warnings](#selectionMixin)

### [`TaskBoard.vue`](src/components/tasks/TaskBoard.vue)

- ℹ️ **dragDropMixin** skipped — Mixin 'dragDropMixin' was NOT migrated: no members are referenced by the component.
- 🔴 [`useSelection`](src/composables/useSelection.js) — 1 error → [See warnings](#selectionMixin)

### [`TaskCard.vue`](src/components/tasks/TaskCard.vue)

- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`TaskComments.vue`](src/components/tasks/TaskComments.vue)

- 🔴 [`useComment`](src/composables/useComment.js) — 3 errors → [See warnings](#commentMixin)
- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`TaskDetail.vue`](src/components/tasks/TaskDetail.vue)

- 🔴 [`useComment`](src/composables/useComment.js) — 3 errors → [See warnings](#commentMixin)
- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`TaskFilters.vue`](src/components/tasks/TaskFilters.vue)

- 🔴 [`useFilter`](src/composables/useFilter.js) — 1 error → [See warnings](#filterMixin)

### [`TaskForm.vue`](src/components/tasks/TaskForm.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors, 1 warning → [See warnings](#formMixin)
- 🔴 [`useValidation`](src/composables/useValidation.js) — 2 errors → [See warnings](#validationMixin)

### [`TaskList.vue`](src/components/tasks/TaskList.vue)

- 🔴 [`useSearch`](src/composables/useSearch.js) — 3 errors → [See warnings](#searchMixin)
- 🔴 [`useFilter`](src/composables/useFilter.js) — 1 error → [See warnings](#filterMixin)
- 🟢 [`usePagination`](src/composables/usePagination.js) — No issues

### [`UserAvatar.vue`](src/components/users/UserAvatar.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`UserCard.vue`](src/components/users/UserCard.vue)

- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)

### [`UserForm.vue`](src/components/users/UserForm.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors, 1 warning → [See warnings](#formMixin)
- 🔴 [`useValidation`](src/composables/useValidation.js) — 2 errors, 1 warning → [See warnings](#validationMixin)

### [`UserList.vue`](src/components/users/UserList.vue)

- 🔴 [`useTable`](src/composables/useTable.js) — 2 errors, 1 warning → [See warnings](#tableMixin)
- 🔴 [`useSearch`](src/composables/useSearch.js) — 3 errors → [See warnings](#searchMixin)
- 🔴 [`useSelection`](src/composables/useSelection.js) — 1 error → [See warnings](#selectionMixin)

### [`UserProfile.vue`](src/components/users/UserProfile.vue)

- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors → [See warnings](#formMixin)
- 🟡 [`usePermission`](src/composables/usePermission.js) — 1 warning → [See warnings](#permissionMixin)
- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`CalendarView.vue`](src/views/CalendarView.vue)

- ℹ️ **filterMixin** skipped — Mixin 'filterMixin' was NOT migrated: no members are referenced by the component.
- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues

### [`DashboardView.vue`](src/views/DashboardView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- ℹ️ **chartMixin** skipped — Mixin 'chartMixin' was NOT migrated: it provides lifecycle hooks (mounted, beforeUnmount) but no members are directly referenced by the component.

### [`LoginView.vue`](src/views/LoginView.vue)

- 🔴 [`useAuth`](src/composables/useAuth.js) — 1 error, 1 warning → [See warnings](#authMixin)
- 🔴 [`useForm`](src/composables/useForm.js) — 2 errors, 1 warning → [See warnings](#formMixin)
- ℹ️ **validationMixin** skipped — Mixin 'validationMixin' was NOT migrated: no members are referenced by the component.

### [`NotFoundView.vue`](src/views/NotFoundView.vue)

- ℹ️ **loadingMixin** skipped — Mixin 'loadingMixin' was NOT migrated: no members are referenced by the component.

### [`ProjectDetailView.vue`](src/views/ProjectDetailView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- ℹ️ **permissionMixin** skipped — Mixin 'permissionMixin' was NOT migrated: no members are referenced by the component.

### [`ProjectsView.vue`](src/views/ProjectsView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🔴 [`useSearch`](src/composables/useSearch.js) — 3 errors → [See warnings](#searchMixin)

### [`ReportsView.vue`](src/views/ReportsView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- ℹ️ **chartMixin** skipped — Mixin 'chartMixin' was NOT migrated: it provides lifecycle hooks (mounted, beforeUnmount) but no members are directly referenced by the component.

### [`SettingsView.vue`](src/views/SettingsView.vue)

- ℹ️ **formMixin** skipped — Mixin 'formMixin' was NOT migrated: it provides lifecycle hooks (beforeMount) but no members are directly referenced by the component.
- ℹ️ **validationMixin** skipped — Mixin 'validationMixin' was NOT migrated: no members are referenced by the component.
- ℹ️ **themeMixin** skipped — Mixin 'themeMixin' was NOT migrated: it provides lifecycle hooks (created) but no members are directly referenced by the component.

### [`TaskDetailView.vue`](src/views/TaskDetailView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- ℹ️ **commentMixin** skipped — Mixin 'commentMixin' was NOT migrated: it provides lifecycle hooks (mounted) but no members are directly referenced by the component.

### [`TasksView.vue`](src/views/TasksView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🔴 [`useFilter`](src/composables/useFilter.js) — 1 error → [See warnings](#filterMixin)

### [`UsersView.vue`](src/views/UsersView.vue)

- 🟡 [`useLoading`](src/composables/useLoading.js) — No issues
- 🔴 [`useSearch`](src/composables/useSearch.js) — 3 errors → [See warnings](#searchMixin)


## `src\composables\useModal.js`

```diff
--- a/src\composables\useModal.js
+++ b/src\composables\useModal.js
@@ -1,4 +1,7 @@
-import { ref, computed } from 'vue'
+// Transformation confidence: LOW (2 warnings — see migration report)
+// ❌ MIGRATION [error]: this.$emit is not available in composables
+// ❌ MIGRATION [error]: this.$refs is not available in composables
+import { ref, computed, onMounted, onBeforeUnmount } from 'vue'
 
 export function useModal() {
   const isOpen = ref(false)
@@ -28,6 +31,17 @@
     closeModal()
   }
 
+  function _handleEscapeKey(event) {
+    if (event.key === 'Escape' && isOpen.value) {
+      closeModal()
+    }
+  }
+  onMounted(() => {
+    document.addEventListener('keydown', _handleEscapeKey)
+  })
+  onBeforeUnmount(() => {
+    document.removeEventListener('keydown', _handleEscapeKey)
+  })
   return {
     isOpen,
     modalData,
@@ -36,6 +50,7 @@
     hasData,
     openModal,
     closeModal,
-    confirmModal
+    confirmModal,
+    _handleEscapeKey,
   }
 }
```

## `src\composables\useTable.js`

```diff
--- a/src\composables\useTable.js
+++ b/src\composables\useTable.js
@@ -1,3 +1,7 @@
+// Transformation confidence: LOW (3 warnings — see migration report)
+// ❌ MIGRATION [error]: this.$refs is not available in composables
+// ❌ MIGRATION [error]: this.$el has no composable equivalent
+// ⚠ MIGRATION [warning]: Nested mixins [sortMixin('name'), paginationMixin] — transitive members may be missed
 import { ref, computed } from 'vue'
 
 export function useTable() {
@@ -66,6 +70,7 @@
     hasExpandedRows,
     sortBy,
     toggleRow,
-    getColumnClass
+    getColumnClass,
+    sortDirection,
   }
 }
```

## `src\composables\useSelection.js`

```diff
--- a/src\composables\useSelection.js
+++ b/src\composables\useSelection.js
@@ -1,3 +1,5 @@
+// Transformation confidence: LOW (1 warnings — see migration report)
+// ❌ MIGRATION [error]: this.$emit is not available in composables
 import { ref, computed } from 'vue'
 
 export function useSelection() {
@@ -61,6 +63,7 @@
     deselect,
     toggleSelection,
     selectAll,
-    isSelected
+    isSelected,
+    deselectAll,
   }
 }
```

## `src\composables\usePagination.js`

```diff
--- a/src\composables\usePagination.js
+++ b/src\composables\usePagination.js
@@ -1,3 +1,4 @@
+// Transformation confidence: HIGH (0 warnings — see migration report)
 import { ref, computed } from 'vue'
 
 export function usePagination() {
@@ -28,8 +29,13 @@
     currentPage.value = 1
   }
 
-  // NOTE: hasPrevPage and prevPage are NOT defined in this composable
 
+  function prevPage() {
+    if (hasPrevPage.value) {
+      currentPage.value--
+    }
+  }
+  const hasPrevPage = computed(() => currentPage.value > 1)
   return {
     currentPage,
     pageSize,
@@ -39,6 +45,8 @@
     paginatedOffset,
     nextPage,
     goToPage,
-    changePageSize
+    changePageSize,
+    prevPage,
+    hasPrevPage,
   }
 }
```

## `src\composables\useForm.js`

```diff
--- a/src\composables\useForm.js
+++ b/src\composables\useForm.js
@@ -1,4 +1,7 @@
-import { ref, computed } from 'vue'
+// Transformation confidence: LOW (2 warnings — see migration report)
+// ❌ MIGRATION [error]: this.$emit is not available in composables
+// ❌ MIGRATION [error]: this.$refs is not available in composables
+import { ref, computed, onBeforeMount } from 'vue'
 
 export function useForm() {
   const formData = ref({})
@@ -40,6 +43,14 @@
 
   // NOTE: setFieldError and dirtyFields are NOT defined in this composable
 
+  const dirtyFields = computed(() => {
+    return Object.keys(formData.value).filter((key) => {
+            return JSON.stringify(formData.value[key]) !== JSON.stringify(originalData.value[key])
+          })
+  })
+  onBeforeMount(() => {
+    initForm(formData.value)
+  })
   return {
     formData,
     originalData,
@@ -50,6 +61,7 @@
     isFormValid,
     initForm,
     resetForm,
-    submitForm
+    submitForm,
+    dirtyFields,
   }
 }
```

## `src\composables\useChart.js`

```diff
--- a/src\composables\useChart.js
+++ b/src\composables\useChart.js
@@ -1,4 +1,7 @@
-import { ref, computed } from 'vue'
+// Transformation confidence: LOW (2 warnings — see migration report)
+// ❌ MIGRATION [error]: this.$refs is not available in composables
+// ❌ MIGRATION [error]: this.$el has no composable equivalent
+import { ref, computed, onMounted, onBeforeUnmount } from 'vue'
 
 export function useChart() {
   const chartData = ref(null)
@@ -54,6 +57,12 @@
     }
   }
 
+  onMounted(() => {
+    resizeChart()
+  })
+  onBeforeUnmount(() => {
+    isChartReady.value = false
+  })
   return {
     chartData,
     chartOptions,
```

## `src\composables\usePermission.js`

```diff
--- a/src\composables\usePermission.js
+++ b/src\composables\usePermission.js
@@ -1,3 +1,5 @@
+// Transformation confidence: MEDIUM (1 warnings — see migration report)
+// ⚠ MIGRATION [warning]: this.$router is not available in composables
 import { ref, computed } from 'vue'
 
 export function usePermission() {
@@ -33,6 +35,7 @@
 
   // NOTE: canDelete and hasRole are NOT defined in this composable
 
+  const canDelete = computed(() => checkPermission('delete'))
   return {
     userPermissions,
     roleMap,
@@ -41,6 +44,7 @@
     isManager,
     permissionLevel,
     checkPermission,
-    requestPermission
+    requestPermission,
+    canDelete,
   }
 }
```

## `src\composables\useTheme.js`

**New file**

```javascript
// Transformation confidence: LOW (2 warnings — see migration report)
import { ref, computed, watch } from 'vue'

export function useTheme() {
  const currentTheme = ref('light')
  const themes = ref(['light', 'dark', 'blue'])
  const customColors = ref({})

  const themeClass = computed(() => 'theme-' + currentTheme.value)
  const isDarkMode = computed(() => currentTheme.value === 'dark')
  const themeVariables = computed(() => {
    const variables = {
            light: {
              '--bg-color': '#ffffff',
              '--text-color': '#333333',
              '--primary-color': '#4CAF50',
              '--border-color': '#e0e0e0'
            },
            dark: {
              '--bg-color': '#1a1a1a',
              '--text-color': '#f0f0f0',
              '--primary-color': '#66BB6A',
              '--border-color': '#444444'
            },
            blue: {
              '--bg-color': '#e3f2fd',
              '--text-color': '#1a237e',
              '--primary-color': '#2196F3',
              '--border-color': '#90caf9'
            }
          }
          return variables[currentTheme.value] || variables.light
  })

  function setTheme(name) {
    if (themes.value.includes(name)) {
      currentTheme.value = name
      localStorage.setItem('selectedTheme', name)
    }
  }
  function toggleDarkMode() {
    if (currentTheme.value === 'dark') {
      setTheme('light')
    } else {
      setTheme('dark')
    }
  }
  function applyTheme() {
    // Remove existing theme classes
    themes.value.forEach(theme => {
      document.documentElement.classList.remove('theme-' + theme)
    })
    // Apply current theme class
    document.documentElement.classList.add('theme-' + currentTheme.value)
    
    // Apply CSS variables
    const vars = themeVariables.value
    Object.keys(vars).forEach(key => {
      document.documentElement.style.setProperty(key, vars[key])
    })
    
    // ❌ MIGRATION [error]: this.$el has no composable equivalent
    if (this.$el && this.$el.style) {
      this.$el.style.transition = 'background-color 0.3s ease'
    }
    
    // ℹ MIGRATION [info]: $forceUpdate — rarely needed in Vue 3
    this.$forceUpdate()
  }

  watch(currentTheme, () => { applyTheme() })

  const savedTheme = localStorage.getItem('selectedTheme')
  if (savedTheme && themes.value.includes(savedTheme)) {
    currentTheme.value = savedTheme
  }

  return { currentTheme, themes, customColors, themeClass, isDarkMode, themeVariables, setTheme, toggleDarkMode, applyTheme }
}
```

## `src\composables\useKeyboardShortcut.js`

**New file**

```javascript
// Transformation confidence: LOW (2 warnings — see migration report)
import { ref, onMounted, onBeforeUnmount } from 'vue'

export function useKeyboardShortcut() {
  const shortcuts = ref({})
  const isShortcutsEnabled = ref(true)

  function registerShortcut(key, handler) {
    shortcuts.value[key] = handler
  }
  function unregisterShortcut(key) {
    delete shortcuts.value[key]
  }
  function handleKeyDown(event) {
    if (!isShortcutsEnabled.value) return
    
    const handler = shortcuts.value[event.key]
    if (handler) {
      handler(event)
      // ❌ MIGRATION [error]: this.$emit is not available in composables
      this.$emit('shortcut-triggered', event.key)
    }
  }

  onMounted(() => {
    document.addEventListener('keydown', handleKeyDown)
    // Reference to component element for focus management
    // ❌ MIGRATION [error]: this.$el has no composable equivalent
    if (this.$el) {
      this.$el.setAttribute('data-shortcuts-enabled', 'true')
    }
  })
  onBeforeUnmount(() => {
    document.removeEventListener('keydown', handleKeyDown)
  })

  return { shortcuts, isShortcutsEnabled, registerShortcut, unregisterShortcut, handleKeyDown }
}
```

## `src\composables\useExport.js`

**New file**

```javascript
// Transformation confidence: HIGH (0 warnings — see migration report)
import { ref, computed } from 'vue'

export function useExport() {
  const exportFormat = ref('csv')
  const exportColumns = ref([])
  const isExporting = ref(false)
  const exportProgress = ref(0)

  const availableFormats = computed(() => ['csv', 'pdf', 'xlsx'])
  const exportFileName = computed(() => 'export_' + Date.now() + '.' + exportFormat.value)

  function exportToCSV(data) {
    isExporting.value = true
    exportProgress.value = 0
    
    const columns = exportColumns.value.length > 0
      ? exportColumns.value
      : Object.keys(data[0] || {})
    
    const header = columns.join(',')
    const rows = data.map(row => {
      return columns.map(col => {
        const value = row[col]
        if (typeof value === 'string' && value.includes(',')) {
          return '"' + value + '"'
        }
        return value
      }).join(',')
    })
    
    const csv = [header, ...rows].join('\n')
    const blob = new Blob([csv], { type: 'text/csv' })
    
    exportProgress.value = 100
    isExporting.value = false
    downloadFile(blob, exportFileName.value)
  }
  function exportToPDF(data) {
    isExporting.value = true
    exportProgress.value = 0
    
    // Simulate PDF export processing
    const preparedData = prepareExportData(data)
    exportProgress.value = 50
    
    // Simulated PDF blob creation
    const blob = new Blob([JSON.stringify(preparedData)], { type: 'application/pdf' })
    
    exportProgress.value = 100
    isExporting.value = false
    downloadFile(blob, exportFileName.value)
  }
  function downloadFile(blob, name) {
    const url = URL.createObjectURL(blob)
    const link = document.createElement('a')
    link.href = url
    link.download = name
    document.body.appendChild(link)
    link.click()
    document.body.removeChild(link)
    URL.revokeObjectURL(url)
  }
  function prepareExportData(data) {
    if (exportColumns.value.length === 0) {
      return data
    }
    return data.map(row => {
      const filtered = {}
      exportColumns.value.forEach(col => {
        if (row[col] !== undefined) {
          filtered[col] = row[col]
        }
      })
      return filtered
    })
  }

  return { exportFormat, exportColumns, isExporting, exportProgress, availableFormats, exportFileName, exportToCSV, exportToPDF, downloadFile, prepareExportData }
}
```

## `src\composables\useFilter.js`

**New file**

```javascript
// Transformation confidence: LOW (1 warnings — see migration report)
import { ref, computed, watch } from 'vue'

export function useFilter() {
  const filters = ref({})
  const activeFilters = ref({})
  const filterOptions = ref({})

  const activeFilterCount = computed(() => Object.keys(activeFilters.value).length)
  const isFiltered = computed(() => activeFilterCount.value > 0)
  const appliedFilterSummary = computed(() => Object.keys(activeFilters.value).join(', '))

  function applyFilter(key, value) {
    activeFilters.value = { ...activeFilters.value, [key]: value }
  }
  function removeFilter(key) {
    const { [key]: _, ...remaining } = activeFilters.value
    activeFilters.value = remaining
  }
  function clearAllFilters() {
    activeFilters.value = {}
  }
  function getFilterOptions(key) {
    return filterOptions.value[key] || []
  }

  // ❌ MIGRATION [error]: this.$emit is not available in composables
  watch(activeFilters, (newFilters) => { this.$emit('filters-changed', newFilters) }, { deep: true })

  return { filters, activeFilters, filterOptions, activeFilterCount, isFiltered, appliedFilterSummary, applyFilter, removeFilter, clearAllFilters, getFilterOptions }
}
```

## `src\composables\useUndoRedo.js`

**New file**

```javascript
// Transformation confidence: LOW (1 warnings — see migration report)
import { ref, computed } from 'vue'

export function useUndoRedo() {
  const undoStack = ref([])
  const redoStack = ref([])
  const maxHistory = ref(50)

  const canUndo = computed(() => undoStack.value.length > 0)
  const canRedo = computed(() => redoStack.value.length > 0)
  const historyLength = computed(() => undoStack.value.length)

  function pushState(state) {
    undoStack.value.push(state)
    if (undoStack.value.length > maxHistory.value) {
      undoStack.value.shift()
    }
    redoStack.value = []
    // ❌ MIGRATION [error]: this.$emit is not available in composables
    this.$emit('state-changed')
  }
  function undo() {
    if (undoStack.value.length === 0) return null
    
    const state = undoStack.value.pop()
    redoStack.value.push(state)
    this.$emit('state-changed')
    return state
  }
  function redo() {
    if (redoStack.value.length === 0) return null
    
    const state = redoStack.value.pop()
    undoStack.value.push(state)
    this.$emit('state-changed')
    return state
  }
  function clearHistory() {
    undoStack.value = []
    redoStack.value = []
  }

  return { undoStack, redoStack, maxHistory, canUndo, canRedo, historyLength, pushState, undo, redo, clearHistory }
}
```

## `src\composables\useNotification.js`

**New file**

```javascript
// Transformation confidence: LOW (1 warnings — see migration report)
import { ref, computed, nextTick } from 'vue'

export function useNotification() {
  const notifications = ref([])
  const unreadCount = ref(0)
  const notificationSettings = ref({
        sound: true,
        desktop: true,
        email: false
      })

  const hasUnread = computed(() => unreadCount.value > 0)
  const sortedNotifications = computed(() => {
    return [...notifications.value].sort((a, b) => {
            return new Date(b.createdAt) - new Date(a.createdAt)
          })
  })
  const groupedByType = computed(() => {
    return notifications.value.reduce((groups, notification) => {
            const type = notification.type || 'general'
            if (!groups[type]) {
              groups[type] = []
            }
            groups[type].push(notification)
            return groups
          }, {})
  })

  function addNotification(n) {
    notifications.value.unshift(n)
    unreadCount.value++
    // ❌ MIGRATION [error]: this.$emit is not available in composables
    this.$emit('notification-added')
    nextTick(() => {
      // DOM updated after notification added
    })
  }
  function markAsRead(id) {
    const notification = notifications.value.find(n => n.id === id)
    if (notification && !notification.read) {
      notification.read = true
      unreadCount.value--
    }
  }
  function markAllRead() {
    notifications.value.forEach(n => {
      n.read = true
    })
    unreadCount.value = 0
  }
  function removeNotification(id) {
    notifications.value = notifications.value.filter(n => n.id !== id)
  }
  function clearAll() {
    notifications.value = []
    unreadCount.value = 0
  }

  // Simulate loading notification settings
  const savedSettings = localStorage.getItem('notificationSettings')
  if (savedSettings) {
    notificationSettings.value = JSON.parse(savedSettings)
  }

  return { notifications, unreadCount, notificationSettings, hasUnread, sortedNotifications, groupedByType, addNotification, markAsRead, markAllRead, removeNotification, clearAll }
}
```

## `src\composables\useDragDrop.js`

**New file**

```javascript
// Transformation confidence: LOW (3 warnings — see migration report)
import { ref, computed } from 'vue'

export function useDragDrop() {
  const isDragging = ref(false)
  const dragItem = ref(null)
  const dropTarget = ref(null)
  const dragOverIndex = ref(-1)

  const hasDragItem = computed(() => !!dragItem.value)
  const dropPosition = computed(() => {
    if (dragOverIndex.value < 0) {
            return 'none'
          }
          return dragOverIndex.value
  })

  function onDragStart(event, item) {
    isDragging.value = true
    dragItem.value = item
    event.dataTransfer.effectAllowed = 'move'
    
    // ❌ MIGRATION [error]: this.$refs is not available in composables
    if (this.$refs.dragContainer) {
      this.$refs.dragContainer.classList.add('dragging')
    }
    // ❌ MIGRATION [error]: this.$el has no composable equivalent
    this.$el.classList.add('drag-active')
  }
  function onDragOver(event, index) {
    event.preventDefault()
    dragOverIndex.value = index
  }
  function onDrop(event) {
    event.preventDefault()
    // ❌ MIGRATION [error]: this.$emit is not available in composables
    this.$emit('item-reordered', {
      item: dragItem.value,
      newIndex: dragOverIndex.value
    })
    
    isDragging.value = false
    dragItem.value = null
    dropTarget.value = null
    dragOverIndex.value = -1
  }
  function onDragEnd() {
    isDragging.value = false
    dragItem.value = null
    dropTarget.value = null
    dragOverIndex.value = -1
    
    if (this.$refs.dragContainer) {
      this.$refs.dragContainer.classList.remove('dragging')
    }
    this.$el.classList.remove('drag-active')
  }

  return { isDragging, dragItem, dropTarget, dragOverIndex, hasDragItem, dropPosition, onDragStart, onDragOver, onDrop, onDragEnd }
}
```

## `src\composables\useComment.js`

**New file**

```javascript
// Transformation confidence: LOW (4 warnings — see migration report)
// ⚠ MIGRATION [warning]: MIGRATION: setTimeout used but no clearTimeout cleanup detected.
import { ref, computed, onMounted, nextTick } from 'vue'

export function useComment() {
  const comments = ref([])
  const newComment = ref('')
  const editingComment = ref(null)
  const isLoadingComments = ref(false)

  const commentCount = computed(() => comments.value.length)
  const sortedComments = computed(() => {
    return [...comments.value].sort((a, b) => {
            return new Date(b.createdAt) - new Date(a.createdAt)
          })
  })
  const hasComments = computed(() => comments.value.length > 0)

  function addComment() {
    if (!newComment.value.trim()) return
    
    const comment = {
      id: Date.now(),
      text: newComment.value,
      createdAt: new Date().toISOString(),
      author: 'currentUser',
      parentId: null
    }
    
    comments.value.unshift(comment)
    newComment.value = ''
    
    // ❌ MIGRATION [error]: this.$refs is not available in composables
    this.$refs.commentInput?.focus()
    // ❌ MIGRATION [error]: this.$emit is not available in composables
    this.$emit('comment-added')
    nextTick(() => {
      // DOM updated after comment added
    })
  }
  function editComment(id, text) {
    const comment = comments.value.find(c => c.id === id)
    if (comment) {
      comment.text = text
      comment.updatedAt = new Date().toISOString()
      editingComment.value = null
    }
  }
  function deleteComment(id) {
    comments.value = comments.value.filter(c => c.id !== id)
  }
  function loadComments(entityId) {
    isLoadingComments.value = true
    // Simulate API call to load comments
    setTimeout(() => {
      comments.value = []
      isLoadingComments.value = false
    }, 500)
  }
  function replyToComment(parentId, text) {
    const reply = {
      id: Date.now(),
      text: text,
      createdAt: new Date().toISOString(),
      author: 'currentUser',
      parentId: parentId
    }
    
    comments.value.unshift(reply)
  }

  onMounted(() => {
    // ❌ MIGRATION [error]: 'entityId' — external dep, not available in composable scope.
    // Accept as composable param: this.entityId → entityId.value or entityId.
    if (this.entityId) {
      loadComments(this.entityId)
    }
  })

  return { comments, newComment, editingComment, isLoadingComments, commentCount, sortedComments, hasComments, addComment, editComment, deleteComment, loadComments, replyToComment }
}
```

## `src\composables\useAuth.js`

**New file**

```javascript
// Transformation confidence: LOW (3 warnings — see migration report)
// ⚠ MIGRATION [warning]: MIGRATION: setTimeout used but no clearTimeout cleanup detected.
import { ref, computed } from 'vue'

export function useAuth() {
  const user = ref(null)
  const isAuthenticated = ref(false)
  const authError = ref(null)
  const loginAttempts = ref(0)

  const isAdmin = computed(() => user.value?.role === 'admin')
  const userDisplayName = computed(() => {
    return user.value
            ? user.value.firstName + ' ' + user.value.lastName
            : 'Guest'
  })
  const hasPermission = computed(() => isAuthenticated.value && user.value)

  function login(credentials) {
    try {
      loginAttempts.value++
      user.value = credentials
      isAuthenticated.value = true
      authError.value = null
      // ❌ MIGRATION [error]: this.$emit is not available in composables
      this.$emit('auth-changed')
    } catch (error) {
      authError.value = error.message
    }
  }
  function logout() {
    user.value = null
    isAuthenticated.value = false
    authError.value = null
    // ⚠ MIGRATION [warning]: this.$router is not available in composables
    this.$router.push('/login')
  }
  function checkAuth() {
    const token = localStorage.getItem('token')
    if (token) {
      isAuthenticated.value = true
    }
  }
  function refreshToken() {
    return new Promise((resolve) => {
      setTimeout(() => {
        localStorage.setItem('token', 'refreshed-token-' + Date.now())
        resolve(true)
      }, 100)
    })
  }

  checkAuth()

  return { user, isAuthenticated, authError, loginAttempts, isAdmin, userDisplayName, hasPermission, login, logout, checkAuth, refreshToken }
}
```

## `src\App.vue`

```diff
--- a/src\App.vue
+++ b/src\App.vue
@@ -5,11 +5,15 @@
 </template>
 
 <script>
-import themeMixin from '@/mixins/themeMixin'
 
+import { useTheme } from '@/composables/useTheme'
 export default {
+  setup() {
+    const { themeClass, currentTheme, themes } = useTheme()
+
+    return { themeClass, currentTheme, themes }
+  },
   name: 'App',
-  mixins: [themeMixin],
   created() {
     // Theme is loaded from localStorage by themeMixin's created hook
   }
```

## `src\components\common\BaseModal.vue`

```diff
--- a/src\components\common\BaseModal.vue
+++ b/src\components\common\BaseModal.vue
@@ -19,13 +19,17 @@
 </template>
 
 <script>
-import modalMixin from '@/mixins/modalMixin'
-import keyboardShortcutMixin from '@/mixins/keyboardShortcutMixin'
+import { useKeyboardShortcut } from '@/composables/useKeyboardShortcut'
+import { useModal } from '@/composables/useModal'
 
 export default {
+  setup() {
+    const { isOpen, modalData, modalTitle, closeModal, confirmModal, _handleEscapeKey } = useModal()
+    const { registerShortcut, shortcuts, handleKeyDown } = useKeyboardShortcut()
+
+    return { isOpen, modalData, modalTitle, closeModal, confirmModal, _handleEscapeKey, registerShortcut, shortcuts, handleKeyDown }
+  },
   name: 'BaseModal',
-
-  mixins: [modalMixin, keyboardShortcutMixin],
 
   emits: ['modal-closed', 'modal-confirmed', 'shortcut-triggered'],
```

## `src\components\common\ConfirmDialog.vue`

```diff
--- a/src\components\common\ConfirmDialog.vue
+++ b/src\components\common\ConfirmDialog.vue
@@ -36,12 +36,15 @@
 </template>
 
 <script>
-import modalMixin from '@/mixins/modalMixin'
 
+import { useModal } from '@/composables/useModal'
 export default {
+  setup() {
+    const { isOpen, closeModal, _handleEscapeKey } = useModal()
+
+    return { isOpen, closeModal, _handleEscapeKey }
+  },
   name: 'ConfirmDialog',
-
-  mixins: [modalMixin],
 
   props: {
     title: {
```

## `src\components\common\DataTable.vue`

```diff
--- a/src\components\common\DataTable.vue
+++ b/src\components\common\DataTable.vue
@@ -123,14 +123,12 @@
 </template>
 
 <script>
-import tableMixin from '@/mixins/tableMixin'
-import selectionMixin from '@/mixins/selectionMixin'
-import paginationMixin from '@/mixins/paginationMixin'
+import { usePagination } from '@/composables/usePagination'
+import { useSelection } from '@/composables/useSelection'
+import { useTable } from '@/composables/useTable'
 
 export default {
   name: 'DataTable',
-
-  mixins: [tableMixin, selectionMixin, paginationMixin],
 
   props: {
     data: {
@@ -144,6 +142,13 @@
   },
 
   emits: ['selection-changed'],
+  setup() {
+    const { rows, columns, sortField, sortDirection, sortedRows, visibleColumns, sortBy, getColumnClass } = useTable()
+    const { selectedItems, hasSelection, selectionCount, allSelected, select, toggleSelection, selectAll, deselectAll, isSelected } = useSelection()
+    const { currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage } = usePagination()
+
+    return { rows, columns, sortField, sortDirection, sortedRows, visibleColumns, sortBy, getColumnClass, selectedItems, hasSelection, selectionCount, allSelected, select, toggleSelection, selectAll, deselectAll, isSelected, currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage }
+  },
 
   data() {
     return {
```

## `src\components\common\EmptyState.vue`

```diff
--- a/src\components\common\EmptyState.vue
+++ b/src\components\common\EmptyState.vue
@@ -44,12 +44,15 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
 
+import { useLoading } from '@/composables/useLoading'
 export default {
+  setup() {
+    const { error, hasError, canRetry, retry } = useLoading()
+
+    return { error, hasError, canRetry, retry }
+  },
   name: 'EmptyState',
-
-  mixins: [loadingMixin],
 
   props: {
     title: {
```

## `src\components\common\ExportButton.vue`

```diff
--- a/src\components\common\ExportButton.vue
+++ b/src\components\common\ExportButton.vue
@@ -63,12 +63,10 @@
 </template>
 
 <script>
-import exportMixin from '@/mixins/exportMixin'
 
+import { useExport } from '@/composables/useExport'
 export default {
   name: 'ExportButton',
-
-  mixins: [exportMixin],
 
   props: {
     data: {
@@ -79,6 +77,11 @@
       type: String,
       default: 'export'
     }
+  },
+  setup() {
+    const { exportFormat, isExporting, exportProgress, availableFormats, exportFileName, exportToCSV, exportToPDF } = useExport()
+
+    return { exportFormat, isExporting, exportProgress, availableFormats, exportFileName, exportToCSV, exportToPDF }
   },
 
   data() {
```

## `src\components\common\FileUploader.vue`

```diff
--- a/src\components\common\FileUploader.vue
+++ b/src\components\common\FileUploader.vue
@@ -81,13 +81,17 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import validationMixin from '@/mixins/validationMixin'
+import { useValidation } from '@/composables/useValidation'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'FileUploader',
+  setup() {
+    const { formData, isDirty, initForm } = useForm()
+    const { errors, isValid } = useValidation()
 
-  mixins: [formMixin, validationMixin],
+    return { formData, isDirty, initForm, errors, isValid }
+  },
 
   data() {
     return {
```

## `src\components\common\FilterPanel.vue`

```diff
--- a/src\components\common\FilterPanel.vue
+++ b/src\components\common\FilterPanel.vue
@@ -81,12 +81,10 @@
 </template>
 
 <script>
-import filterMixin from '@/mixins/filterMixin'
 
+import { useFilter } from '@/composables/useFilter'
 export default {
   name: 'FilterPanel',
-
-  mixins: [filterMixin],
 
   props: {
     availableFilters: {
@@ -96,6 +94,11 @@
   },
 
   emits: ['filters-changed'],
+  setup() {
+    const { filters, activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters } = useFilter()
+
+    return { filters, activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters }
+  },
 
   data() {
     return {
```

## `src\components\common\LoadingSpinner.vue`

```diff
--- a/src\components\common\LoadingSpinner.vue
+++ b/src\components\common\LoadingSpinner.vue
@@ -48,12 +48,15 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
 
+import { useLoading } from '@/composables/useLoading'
 export default {
+  setup() {
+    const { isLoading, loadingMessage, error, retryCount, hasError, canRetry, startLoading, retry } = useLoading()
+
+    return { isLoading, loadingMessage, error, retryCount, hasError, canRetry, startLoading, retry }
+  },
   name: 'LoadingSpinner',
-
-  mixins: [loadingMixin],
 
   props: {
     message: {
```

## `src\components\common\PaginationBar.vue`

```diff
--- a/src\components\common\PaginationBar.vue
+++ b/src\components\common\PaginationBar.vue
@@ -80,12 +80,15 @@
 </template>
 
 <script>
-import paginationMixin from '@/mixins/paginationMixin'
 
+import { usePagination } from '@/composables/usePagination'
 export default {
+  setup() {
+    const { currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage, goToPage, changePageSize } = usePagination()
+
+    return { currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage, goToPage, changePageSize }
+  },
   name: 'PaginationBar',
-
-  mixins: [paginationMixin],
 
   props: {
     total: {
```

## `src\components\common\RichTextEditor.vue`

```diff
--- a/src\components\common\RichTextEditor.vue
+++ b/src\components\common\RichTextEditor.vue
@@ -106,13 +106,17 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import undoRedoMixin from '@/mixins/undoRedoMixin'
+import { useUndoRedo } from '@/composables/useUndoRedo'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'RichTextEditor',
-
-  mixins: [formMixin, undoRedoMixin],
+  setup() {
+    const { formData, isDirty, isFormValid, initForm } = useForm()
+    const { canUndo, canRedo, historyLength, pushState, undo, redo } = useUndoRedo()
+
+    return { formData, isDirty, isFormValid, initForm, canUndo, canRedo, historyLength, pushState, undo, redo }
+  },
 
   data() {
     return {
```

## `src\components\common\SearchBar.vue`

```diff
--- a/src\components\common\SearchBar.vue
+++ b/src\components\common\SearchBar.vue
@@ -92,12 +92,10 @@
 </template>
 
 <script>
-import searchMixin from '@/mixins/searchMixin'
 
+import { useSearch } from '@/composables/useSearch'
 export default {
   name: 'SearchBar',
-
-  mixins: [searchMixin],
 
   props: {
     placeholder: {
@@ -111,6 +109,11 @@
   },
 
   emits: ['search-results'],
+  setup() {
+    const { searchQuery, searchResults, isSearching, hasResults, resultCount, recentSearches, search, clearSearch } = useSearch()
+
+    return { searchQuery, searchResults, isSearching, hasResults, resultCount, recentSearches, search, clearSearch }
+  },
 
   data() {
     return {
```

## `src\components\dashboard\ProjectSummaryChart.vue`

```diff
--- a/src\components\dashboard\ProjectSummaryChart.vue
+++ b/src\components\dashboard\ProjectSummaryChart.vue
@@ -51,14 +51,17 @@
 </template>
 
 <script>
-import chartMixin from '@/mixins/chartMixin'
+import { useChart } from '@/composables/useChart'
 import { useProjectsStore } from '@/stores/projects'
 import { useTasksStore } from '@/stores/tasks'
 
 export default {
   name: 'ProjectSummaryChart',
+  setup() {
+    const { chartData, isChartReady, chartColors, hasData, prepareChartData, resizeChart } = useChart()
 
-  mixins: [chartMixin],
+    return { chartData, isChartReady, chartColors, hasData, prepareChartData, resizeChart }
+  },
 
   data() {
     return {
```

## `src\components\dashboard\RecentActivity.vue`

```diff
--- a/src\components\dashboard\RecentActivity.vue
+++ b/src\components\dashboard\RecentActivity.vue
@@ -108,15 +108,19 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
-import paginationMixin from '@/mixins/paginationMixin'
+import { usePagination } from '@/composables/usePagination'
+import { useLoading } from '@/composables/useLoading'
 import { timeAgo } from '@/utils/helpers'
 import { api } from '@/services/api'
 
 export default {
   name: 'RecentActivity',
-
-  mixins: [loadingMixin, paginationMixin],
+  setup() {
+    const { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
+    const { currentPage, pageSize, totalItems, totalPages, hasNextPage, paginatedOffset, nextPage, prevPage } = usePagination()
+
+    return { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry, currentPage, pageSize, totalItems, totalPages, hasNextPage, paginatedOffset, nextPage, prevPage }
+  },
 
   data() {
     return {
```

## `src\components\dashboard\StatsOverview.vue`

```diff
--- a/src\components\dashboard\StatsOverview.vue
+++ b/src\components\dashboard\StatsOverview.vue
@@ -97,16 +97,20 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
-import chartMixin from '@/mixins/chartMixin'
+import { useChart } from '@/composables/useChart'
+import { useLoading } from '@/composables/useLoading'
 import { useProjectsStore } from '@/stores/projects'
 import { useTasksStore } from '@/stores/tasks'
 import { useUsersStore } from '@/stores/users'
 
 export default {
   name: 'StatsOverview',
+  setup() {
+    const { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
+    const { prepareChartData, resizeChart, isChartReady } = useChart()
 
-  mixins: [loadingMixin, chartMixin],
+    return { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry, prepareChartData, resizeChart, isChartReady }
+  },
 
   data() {
     return {
```

## `src\components\dashboard\TaskDistribution.vue`

```diff
--- a/src\components\dashboard\TaskDistribution.vue
+++ b/src\components\dashboard\TaskDistribution.vue
@@ -74,14 +74,18 @@
 </template>
 
 <script>
-import chartMixin from '@/mixins/chartMixin'
-import loadingMixin from '@/mixins/loadingMixin'
+import { useLoading } from '@/composables/useLoading'
+import { useChart } from '@/composables/useChart'
 import { useTasksStore } from '@/stores/tasks'
 
 export default {
   name: 'TaskDistribution',
+  setup() {
+    const { prepareChartData, resizeChart, isChartReady } = useChart()
+    const { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
 
-  mixins: [chartMixin, loadingMixin],
+    return { prepareChartData, resizeChart, isChartReady, isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry }
+  },
 
   data() {
     return {
```

## `src\components\dashboard\UpcomingDeadlines.vue`

```diff
--- a/src\components\dashboard\UpcomingDeadlines.vue
+++ b/src\components\dashboard\UpcomingDeadlines.vue
@@ -98,7 +98,7 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
+import { useLoading } from '@/composables/useLoading'
 import sortMixin from '@/mixins/sortMixin'
 import { formatDate } from '@/utils/helpers'
 import { useTasksStore } from '@/stores/tasks'
@@ -107,7 +107,12 @@
 export default {
   name: 'UpcomingDeadlines',
 
-  mixins: [loadingMixin, sortMixin('dueDate')],
+  mixins: [sortMixin('dueDate')],
+  setup() {
+    const { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
+
+    return { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry }
+  },
 
   data() {
     return {
```

## `src\components\layout\AppBreadcrumb.vue`

```diff
--- a/src\components\layout\AppBreadcrumb.vue
+++ b/src\components\layout\AppBreadcrumb.vue
@@ -75,12 +75,15 @@
 </template>
 
 <script>
-import loadingMixin from '../../mixins/loadingMixin'
 
+import { useLoading } from '@/composables/useLoading'
 export default {
+  setup() {
+    const { isLoading, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
+
+    return { isLoading, hasError, canRetry, startLoading, stopLoading, setError, retry }
+  },
   name: 'AppBreadcrumb',
-
-  mixins: [loadingMixin],
 
   computed: {
     breadcrumbs() {
```

## `src\components\layout\AppFooter.vue`

```diff
--- a/src\components\layout\AppFooter.vue
+++ b/src\components\layout\AppFooter.vue
@@ -54,14 +54,17 @@
 </template>
 
 <script>
-import themeMixin from '../../mixins/themeMixin'
 
+import { useTheme } from '@/composables/useTheme'
 export default {
   name: 'AppFooter',
 
-  mixins: [themeMixin],
+  inheritAttrs: false,
+  setup() {
+    const { isDarkMode, currentTheme, themes } = useTheme()
 
-  inheritAttrs: false,
+    return { isDarkMode, currentTheme, themes }
+  },
 
   data() {
     return {
```

## `src\components\layout\AppHeader.vue`

```diff
--- a/src\components\layout\AppHeader.vue
+++ b/src\components\layout\AppHeader.vue
@@ -137,14 +137,14 @@
 </template>
 
 <script>
+import { useNotification } from '@/composables/useNotification'
 import themeMixin from '../../mixins/themeMixin'
-import notificationMixin from '../../mixins/notificationMixin'
 import ThemeSwitcher from './ThemeSwitcher.vue'
 
 export default {
   name: 'AppHeader',
 
-  mixins: [themeMixin, notificationMixin],
+  mixins: [themeMixin],
 
   components: {
     ThemeSwitcher
@@ -153,6 +153,11 @@
   inheritAttrs: false,
 
   emits: ['toggle-sidebar'],
+  setup() {
+    const { notifications, unreadCount, hasUnread, sortedNotifications, markAsRead, markAllRead, notificationSettings } = useNotification()
+
+    return { notifications, unreadCount, hasUnread, sortedNotifications, markAsRead, markAllRead, notificationSettings }
+  },
 
   data() {
     return {
```

## `src\components\layout\AppLayout.vue`

```diff
--- a/src\components\layout\AppLayout.vue
+++ b/src\components\layout\AppLayout.vue
@@ -21,7 +21,7 @@
 </template>
 
 <script>
-import keyboardShortcutMixin from '../../mixins/keyboardShortcutMixin'
+import { useKeyboardShortcut } from '@/composables/useKeyboardShortcut'
 import AppHeader from './AppHeader.vue'
 import AppSidebar from './AppSidebar.vue'
 import AppFooter from './AppFooter.vue'
@@ -30,13 +30,16 @@
 export default {
   name: 'AppLayout',
 
-  mixins: [keyboardShortcutMixin],
-
   components: {
     AppHeader,
     AppSidebar,
     AppFooter,
     AppBreadcrumb
+  },
+  setup() {
+    const { registerShortcut, shortcuts, handleKeyDown } = useKeyboardShortcut()
+
+    return { registerShortcut, shortcuts, handleKeyDown }
   },
 
   data() {
```

## `src\components\layout\AppSidebar.vue`

```diff
--- a/src\components\layout\AppSidebar.vue
+++ b/src\components\layout\AppSidebar.vue
@@ -58,14 +58,17 @@
 </template>
 
 <script>
-import permissionMixin from '../../mixins/permissionMixin'
 
+import { usePermission } from '@/composables/usePermission'
 export default {
   name: 'AppSidebar',
 
-  mixins: [permissionMixin],
+  emits: ['navigate'],
+  setup() {
+    const { permissionLevel, checkPermission } = usePermission()
 
-  emits: ['navigate'],
+    return { permissionLevel, checkPermission }
+  },
 
   data() {
     return {
```

## `src\components\layout\ThemeSwitcher.vue`

```diff
--- a/src\components\layout\ThemeSwitcher.vue
+++ b/src\components\layout\ThemeSwitcher.vue
@@ -43,12 +43,15 @@
 </template>
 
 <script>
-import themeMixin from '../../mixins/themeMixin'
 
+import { useTheme } from '@/composables/useTheme'
 export default {
+  setup() {
+    const { currentTheme, isDarkMode, toggleDarkMode, applyTheme, themes } = useTheme()
+
+    return { currentTheme, isDarkMode, toggleDarkMode, applyTheme, themes }
+  },
   name: 'ThemeSwitcher',
-
-  mixins: [themeMixin],
 
   emits: ['theme-changed'],
```

## `src\components\notifications\NotificationBell.vue`

```diff
--- a/src\components\notifications\NotificationBell.vue
+++ b/src\components\notifications\NotificationBell.vue
@@ -135,15 +135,18 @@
 </template>
 
 <script>
-import notificationMixin from '@/mixins/notificationMixin'
+import { useNotification } from '@/composables/useNotification'
 import { timeAgo } from '@/utils/helpers'
 
 export default {
   name: 'NotificationBell',
 
-  mixins: [notificationMixin],
-
   emits: ['notification-added', 'notification-clicked', 'view-all'],
+  setup() {
+    const { notifications, unreadCount, hasUnread, sortedNotifications, markAsRead, markAllRead, notificationSettings } = useNotification()
+
+    return { notifications, unreadCount, hasUnread, sortedNotifications, markAsRead, markAllRead, notificationSettings }
+  },
 
   data() {
     return {
```

## `src\components\notifications\NotificationList.vue`

```diff
--- a/src\components\notifications\NotificationList.vue
+++ b/src\components\notifications\NotificationList.vue
@@ -175,16 +175,20 @@
 </template>
 
 <script>
-import notificationMixin from '@/mixins/notificationMixin'
-import paginationMixin from '@/mixins/paginationMixin'
+import { usePagination } from '@/composables/usePagination'
+import { useNotification } from '@/composables/useNotification'
 import { timeAgo } from '@/utils/helpers'
 
 export default {
   name: 'NotificationList',
 
-  mixins: [notificationMixin, paginationMixin],
-
   emits: ['notification-added', 'notification-clicked'],
+  setup() {
+    const { notifications, unreadCount, hasUnread, sortedNotifications, markAsRead, markAllRead, removeNotification, clearAll, notificationSettings } = useNotification()
+    const { currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage } = usePagination()
+
+    return { notifications, unreadCount, hasUnread, sortedNotifications, markAsRead, markAllRead, removeNotification, clearAll, notificationSettings, currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage }
+  },
 
   data() {
     return {
```

## `src\components\projects\ProjectBoard.vue`

```diff
--- a/src\components\projects\ProjectBoard.vue
+++ b/src\components\projects\ProjectBoard.vue
@@ -91,13 +91,11 @@
 </template>
 
 <script>
-import dragDropMixin from '@/mixins/dragDropMixin'
-import selectionMixin from '@/mixins/selectionMixin'
+import { useSelection } from '@/composables/useSelection'
+import { useDragDrop } from '@/composables/useDragDrop'
 
 export default {
   name: 'ProjectBoard',
-
-  mixins: [dragDropMixin, selectionMixin],
 
   props: {
     projects: {
@@ -107,6 +105,12 @@
   },
 
   emits: ['project-moved', 'item-reordered', 'selection-changed'],
+  setup() {
+    const { isDragging, dragItem, dragOverIndex, onDragStart, onDragOver, onDrop, onDragEnd } = useDragDrop()
+    const { toggleSelection, isSelected } = useSelection()
+
+    return { isDragging, dragItem, dragOverIndex, onDragStart, onDragOver, onDrop, onDragEnd, toggleSelection, isSelected }
+  },
 
   data() {
     return {
```

## `src\components\projects\ProjectCard.vue`

```diff
--- a/src\components\projects\ProjectCard.vue
+++ b/src\components\projects\ProjectCard.vue
@@ -76,13 +76,16 @@
 </template>
 
 <script>
-import permissionMixin from '@/mixins/permissionMixin'
+import { usePermission } from '@/composables/usePermission'
 import { getStatusColor } from '@/utils/helpers'
 
 export default {
+  setup() {
+    const { canEdit, canDelete } = usePermission()
+
+    return { canEdit, canDelete }
+  },
   name: 'ProjectCard',
-
-  mixins: [permissionMixin],
 
   props: {
     project: {
```

## `src\components\projects\ProjectForm.vue`

```diff
--- a/src\components\projects\ProjectForm.vue
+++ b/src\components\projects\ProjectForm.vue
@@ -128,13 +128,11 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import validationMixin from '@/mixins/validationMixin'
+import { useValidation } from '@/composables/useValidation'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'ProjectForm',
-
-  mixins: [formMixin, validationMixin],
 
   props: {
     project: {
@@ -144,6 +142,12 @@
   },
 
   emits: ['form-submitted'],
+  setup() {
+    const { formData, isSubmitting, isFormValid, initForm, resetForm } = useForm()
+    const { rules, errors, errorCount, validate, validateField } = useValidation()
+
+    return { formData, isSubmitting, isFormValid, initForm, resetForm, rules, errors, errorCount, validate, validateField }
+  },
 
   data() {
     return {
```

## `src\components\projects\ProjectList.vue`

```diff
--- a/src\components\projects\ProjectList.vue
+++ b/src\components\projects\ProjectList.vue
@@ -117,19 +117,25 @@
 </template>
 
 <script>
+import { usePagination } from '@/composables/usePagination'
+import { useFilter } from '@/composables/useFilter'
 import sortMixin from '@/mixins/sortMixin'
-import filterMixin from '@/mixins/filterMixin'
-import paginationMixin from '@/mixins/paginationMixin'
 import ProjectCard from './ProjectCard.vue'
 
 export default {
+  setup() {
+    const { activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters } = useFilter()
+    const { currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage, goToPage } = usePagination()
+
+    return { activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters, currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage, goToPage }
+  },
   name: 'ProjectList',
 
   components: {
     ProjectCard
   },
 
-  mixins: [sortMixin('created_at'), filterMixin, paginationMixin],
+  mixins: [sortMixin('created_at')],
 
   props: {
     projects: {
```

## `src\components\projects\ProjectMembers.vue`

```diff
--- a/src\components\projects\ProjectMembers.vue
+++ b/src\components\projects\ProjectMembers.vue
@@ -113,13 +113,17 @@
 </template>
 
 <script>
-import selectionMixin from '@/mixins/selectionMixin'
-import permissionMixin from '@/mixins/permissionMixin'
+import { usePermission } from '@/composables/usePermission'
+import { useSelection } from '@/composables/useSelection'
 
 export default {
+  setup() {
+    const { hasSelection, selectionCount, toggleSelection, deselectAll, isSelected } = useSelection()
+    const { canDelete, canCreate } = usePermission()
+
+    return { hasSelection, selectionCount, toggleSelection, deselectAll, isSelected, canDelete, canCreate }
+  },
   name: 'ProjectMembers',
-
-  mixins: [selectionMixin, permissionMixin],
 
   props: {
     members: {
```

## `src\components\projects\ProjectSettings.vue`

```diff
--- a/src\components\projects\ProjectSettings.vue
+++ b/src\components\projects\ProjectSettings.vue
@@ -188,15 +188,19 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import permissionMixin from '@/mixins/permissionMixin'
+import { usePermission } from '@/composables/usePermission'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'ProjectSettings',
 
-  mixins: [formMixin, permissionMixin],
-
   emits: ['form-submitted', 'delete-project'],
+  setup() {
+    const { formData, isSubmitting, formErrors, hasChanges, dirtyFields, resetForm, submitForm, initForm } = useForm()
+    const { canEdit, canDelete, permissionLevel } = usePermission()
+
+    return { formData, isSubmitting, formErrors, hasChanges, dirtyFields, resetForm, submitForm, initForm, canEdit, canDelete, permissionLevel }
+  },
 
   data() {
     return {
```

## `src\components\projects\ProjectTimeline.vue`

```diff
--- a/src\components\projects\ProjectTimeline.vue
+++ b/src\components\projects\ProjectTimeline.vue
@@ -116,20 +116,24 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
-import chartMixin from '@/mixins/chartMixin'
+import { useChart } from '@/composables/useChart'
+import { useLoading } from '@/composables/useLoading'
 import { useTasksStore } from '@/stores/tasks'
 
 export default {
   name: 'ProjectTimeline',
-
-  mixins: [loadingMixin, chartMixin],
 
   props: {
     projectId: {
       type: [String, Number],
       default: null
     }
+  },
+  setup() {
+    const { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
+    const { hasData, prepareChartData, resizeChart, isChartReady } = useChart()
+
+    return { isLoading, error, hasError, canRetry, startLoading, stopLoading, setError, retry, hasData, prepareChartData, resizeChart, isChartReady }
   },
 
   data() {
```

## `src\components\reports\ReportBuilder.vue`

```diff
--- a/src\components\reports\ReportBuilder.vue
+++ b/src\components\reports\ReportBuilder.vue
@@ -210,16 +210,21 @@
 </template>
 
 <script>
-import chartMixin from '@/mixins/chartMixin'
-import exportMixin from '@/mixins/exportMixin'
-import filterMixin from '@/mixins/filterMixin'
+import { useFilter } from '@/composables/useFilter'
+import { useExport } from '@/composables/useExport'
+import { useChart } from '@/composables/useChart'
 
 export default {
   name: 'ReportBuilder',
 
-  mixins: [chartMixin, exportMixin, filterMixin],
-
   emits: ['filters-changed'],
+  setup() {
+    const { chartType, chartColors, prepareChartData, resizeChart, isChartReady } = useChart()
+    const { isExporting, exportProgress, exportToCSV } = useExport()
+    const { filters, activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters } = useFilter()
+
+    return { chartType, chartColors, prepareChartData, resizeChart, isChartReady, isExporting, exportProgress, exportToCSV, filters, activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters }
+  },
 
   data() {
     return {
```

## `src\components\reports\ReportChart.vue`

```diff
--- a/src\components\reports\ReportChart.vue
+++ b/src\components\reports\ReportChart.vue
@@ -164,12 +164,10 @@
 </template>
 
 <script>
-import chartMixin from '@/mixins/chartMixin'
-
+
+import { useChart } from '@/composables/useChart'
 export default {
   name: 'ReportChart',
-
-  mixins: [chartMixin],
 
   props: {
     data: {
@@ -181,6 +179,11 @@
       default: 'bar',
       validator: (value) => ['bar', 'pie', 'line'].includes(value)
     }
+  },
+  setup() {
+    const { chartColors, prepareChartData, resizeChart, isChartReady } = useChart()
+
+    return { chartColors, prepareChartData, resizeChart, isChartReady }
   },
 
   data() {
```

## `src\components\reports\ReportExport.vue`

```diff
--- a/src\components\reports\ReportExport.vue
+++ b/src\components\reports\ReportExport.vue
@@ -154,12 +154,10 @@
 </template>
 
 <script>
-import exportMixin from '@/mixins/exportMixin'
-
+
+import { useExport } from '@/composables/useExport'
 export default {
   name: 'ReportExport',
-
-  mixins: [exportMixin],
 
   props: {
     data: {
@@ -173,6 +171,11 @@
   },
 
   emits: ['export-started', 'export-complete'],
+  setup() {
+    const { isExporting, exportProgress, availableFormats, exportFileName, exportToCSV, exportToPDF } = useExport()
+
+    return { isExporting, exportProgress, availableFormats, exportFileName, exportToCSV, exportToPDF }
+  },
 
   data() {
     return {
```

## `src\components\reports\ReportTable.vue`

```diff
--- a/src\components\reports\ReportTable.vue
+++ b/src\components\reports\ReportTable.vue
@@ -139,13 +139,11 @@
 </template>
 
 <script>
-import tableMixin from '@/mixins/tableMixin'
-import exportMixin from '@/mixins/exportMixin'
+import { useExport } from '@/composables/useExport'
+import { useTable } from '@/composables/useTable'
 
 export default {
   name: 'ReportTable',
-
-  mixins: [tableMixin, exportMixin],
 
   props: {
     data: {
@@ -156,6 +154,12 @@
       type: Array,
       default: () => []
     }
+  },
+  setup() {
+    const { rows, columns, sortField, sortDirection, sortBy } = useTable()
+    const { exportColumns, isExporting, exportProgress, exportToCSV, exportToPDF } = useExport()
+
+    return { rows, columns, sortField, sortDirection, sortBy, exportColumns, isExporting, exportProgress, exportToCSV, exportToPDF }
   },
 
   data() {
```

## `src\components\tasks\KanbanColumn.vue`

```diff
--- a/src\components\tasks\KanbanColumn.vue
+++ b/src\components\tasks\KanbanColumn.vue
@@ -53,14 +53,14 @@
 </template>
 
 <script>
+import { useSelection } from '@/composables/useSelection'
 import dragDropMixin from '@/mixins/dragDropMixin'
-import selectionMixin from '@/mixins/selectionMixin'
 import TaskCard from './TaskCard.vue'
 
 export default {
   name: 'KanbanColumn',
 
-  mixins: [dragDropMixin, selectionMixin],
+  mixins: [dragDropMixin],
 
   components: {
     TaskCard
@@ -79,6 +79,11 @@
       type: Array,
       required: true
     }
+  },
+  setup() {
+    const { hasSelection, selectionCount, toggleSelection } = useSelection()
+
+    return { hasSelection, selectionCount, toggleSelection }
   },
 
   data() {
```

## `src\components\tasks\TaskBoard.vue`

```diff
--- a/src\components\tasks\TaskBoard.vue
+++ b/src\components\tasks\TaskBoard.vue
@@ -30,15 +30,15 @@
 </template>
 
 <script>
+import { useSelection } from '@/composables/useSelection'
 import dragDropMixin from '@/mixins/dragDropMixin'
-import selectionMixin from '@/mixins/selectionMixin'
 import { useTasksStore } from '@/stores/tasks'
 import KanbanColumn from './KanbanColumn.vue'
 
 export default {
   name: 'TaskBoard',
 
-  mixins: [dragDropMixin, selectionMixin],
+  mixins: [dragDropMixin],
 
   components: {
     KanbanColumn
@@ -49,6 +49,11 @@
       type: Array,
       required: true
     }
+  },
+  setup() {
+    const { hasSelection, selectionCount, select } = useSelection()
+
+    return { hasSelection, selectionCount, select }
   },
 
   data() {
```

## `src\components\tasks\TaskComments.vue`

```diff
--- a/src\components\tasks\TaskComments.vue
+++ b/src\components\tasks\TaskComments.vue
@@ -189,13 +189,11 @@
 </template>
 
 <script>
-import commentMixin from '@/mixins/commentMixin'
-import loadingMixin from '@/mixins/loadingMixin'
+import { useLoading } from '@/composables/useLoading'
+import { useComment } from '@/composables/useComment'
 
 export default {
   name: 'TaskComments',
-
-  mixins: [commentMixin, loadingMixin],
 
   props: {
     taskId: {
@@ -206,6 +204,12 @@
       type: [String, Number],
       default: null
     }
+  },
+  setup() {
+    const { comments, newComment, editingComment, isLoadingComments, commentCount, sortedComments, hasComments, addComment, editComment, deleteComment, loadComments, replyToComment } = useComment()
+    const { isLoading, loadingMessage, error, hasError, canRetry, retry } = useLoading()
+
+    return { comments, newComment, editingComment, isLoadingComments, commentCount, sortedComments, hasComments, addComment, editComment, deleteComment, loadComments, replyToComment, isLoading, loadingMessage, error, hasError, canRetry, retry }
   },
 
   data() {
```

## `src\components\tasks\TaskDetail.vue`

```diff
--- a/src\components\tasks\TaskDetail.vue
+++ b/src\components\tasks\TaskDetail.vue
@@ -231,21 +231,26 @@
 </template>
 
 <script>
-import commentMixin from '@/mixins/commentMixin'
-import loadingMixin from '@/mixins/loadingMixin'
-import permissionMixin from '@/mixins/permissionMixin'
+import { usePermission } from '@/composables/usePermission'
+import { useLoading } from '@/composables/useLoading'
+import { useComment } from '@/composables/useComment'
 import { api } from '@/services/api'
 
 export default {
   name: 'TaskDetail',
-
-  mixins: [commentMixin, loadingMixin, permissionMixin],
 
   props: {
     taskId: {
       type: [String, Number],
       required: true
     }
+  },
+  setup() {
+    const { comments, newComment, isLoadingComments, commentCount, sortedComments, hasComments, addComment, deleteComment, loadComments } = useComment()
+    const { isLoading, loadingMessage, error, hasError, canRetry, startLoading, stopLoading, setError, retry } = useLoading()
+    const { canEdit, canDelete } = usePermission()
+
+    return { comments, newComment, isLoadingComments, commentCount, sortedComments, hasComments, addComment, deleteComment, loadComments, isLoading, loadingMessage, error, hasError, canRetry, startLoading, stopLoading, setError, retry, canEdit, canDelete }
   },
 
   data() {
```

## `src\components\tasks\TaskFilters.vue`

```diff
--- a/src\components\tasks\TaskFilters.vue
+++ b/src\components\tasks\TaskFilters.vue
@@ -136,18 +136,21 @@
 </template>
 
 <script>
-import filterMixin from '@/mixins/filterMixin'
-
+
+import { useFilter } from '@/composables/useFilter'
 export default {
   name: 'TaskFilters',
-
-  mixins: [filterMixin],
 
   props: {
     users: {
       type: Array,
       default: () => []
     }
+  },
+  setup() {
+    const { filters, activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters } = useFilter()
+
+    return { filters, activeFilters, activeFilterCount, isFiltered, applyFilter, removeFilter, clearAllFilters }
   },
 
   data() {
```

## `src\components\tasks\TaskForm.vue`

```diff
--- a/src\components\tasks\TaskForm.vue
+++ b/src\components\tasks\TaskForm.vue
@@ -195,13 +195,11 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import validationMixin from '@/mixins/validationMixin'
+import { useValidation } from '@/composables/useValidation'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'TaskForm',
-
-  mixins: [formMixin, validationMixin],
 
   props: {
     task: {
@@ -215,6 +213,12 @@
   },
 
   emits: ['task-created', 'task-updated'],
+  setup() {
+    const { formData, isDirty, isSubmitting, hasChanges, resetForm, submitForm, initForm } = useForm()
+    const { rules, errors, firstError, errorCount, validate, validateField } = useValidation()
+
+    return { formData, isDirty, isSubmitting, hasChanges, resetForm, submitForm, initForm, rules, errors, firstError, errorCount, validate, validateField }
+  },
 
   data() {
     return {
```

## `src\components\tasks\TaskList.vue`

```diff
--- a/src\components\tasks\TaskList.vue
+++ b/src\components\tasks\TaskList.vue
@@ -155,15 +155,13 @@
 </template>
 
 <script>
-import searchMixin from '@/mixins/searchMixin'
-import filterMixin from '@/mixins/filterMixin'
-import paginationMixin from '@/mixins/paginationMixin'
+import { usePagination } from '@/composables/usePagination'
+import { useFilter } from '@/composables/useFilter'
+import { useSearch } from '@/composables/useSearch'
 import TaskCard from './TaskCard.vue'
 
 export default {
   name: 'TaskList',
-
-  mixins: [searchMixin, filterMixin, paginationMixin],
 
   components: {
     TaskCard
@@ -174,6 +172,13 @@
       type: Array,
       required: true
     }
+  },
+  setup() {
+    const { searchQuery, searchResults, resultCount, search, clearSearch } = useSearch()
+    const { filters, activeFilters, activeFilterCount, isFiltered, applyFilter, clearAllFilters } = useFilter()
+    const { currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage, goToPage } = usePagination()
+
+    return { searchQuery, searchResults, resultCount, search, clearSearch, filters, activeFilters, activeFilterCount, isFiltered, applyFilter, clearAllFilters, currentPage, pageSize, totalItems, totalPages, hasPrevPage, hasNextPage, paginatedOffset, nextPage, prevPage, goToPage }
   },
 
   data() {
```

## `src\components\users\UserAvatar.vue`

```diff
--- a/src\components\users\UserAvatar.vue
+++ b/src\components\users\UserAvatar.vue
@@ -37,12 +37,10 @@
 </template>
 
 <script>
-import loadingMixin from '@/mixins/loadingMixin'
 
+import { useLoading } from '@/composables/useLoading'
 export default {
   name: 'UserAvatar',
-
-  mixins: [loadingMixin],
 
   inheritAttrs: false,
 
@@ -60,6 +58,11 @@
       type: Boolean,
       default: false
     }
+  },
+  setup() {
+    const { isLoading, error, stopLoading } = useLoading()
+
+    return { isLoading, error, stopLoading }
   },
 
   data() {
```

## `src\components\users\UserCard.vue`

```diff
--- a/src\components\users\UserCard.vue
+++ b/src\components\users\UserCard.vue
@@ -91,13 +91,16 @@
 </template>
 
 <script>
-import permissionMixin from '@/mixins/permissionMixin'
+import { usePermission } from '@/composables/usePermission'
 import { capitalize } from '@/utils/helpers'
 
 export default {
+  setup() {
+    const { canEdit, canDelete } = usePermission()
+
+    return { canEdit, canDelete }
+  },
   name: 'UserCard',
-
-  mixins: [permissionMixin],
 
   props: {
     user: {
```

## `src\components\users\UserForm.vue`

```diff
--- a/src\components\users\UserForm.vue
+++ b/src\components\users\UserForm.vue
@@ -169,13 +169,11 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import validationMixin from '@/mixins/validationMixin'
+import { useValidation } from '@/composables/useValidation'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'UserForm',
-
-  mixins: [formMixin, validationMixin],
 
   props: {
     user: {
@@ -185,6 +183,12 @@
   },
 
   emits: ['user-saved', 'form-cancelled', 'form-submitted', 'validation-complete'],
+  setup() {
+    const { formData, isSubmitting, hasChanges, isFormValid, initForm, resetForm } = useForm()
+    const { errors, errorCount, validate, validateField } = useValidation()
+
+    return { formData, isSubmitting, hasChanges, isFormValid, initForm, resetForm, errors, errorCount, validate, validateField }
+  },
 
   data() {
     return {
```

## `src\components\users\UserList.vue`

```diff
--- a/src\components\users\UserList.vue
+++ b/src\components\users\UserList.vue
@@ -209,14 +209,12 @@
 </template>
 
 <script>
-import tableMixin from '@/mixins/tableMixin'
-import searchMixin from '@/mixins/searchMixin'
-import selectionMixin from '@/mixins/selectionMixin'
+import { useSelection } from '@/composables/useSelection'
+import { useSearch } from '@/composables/useSearch'
+import { useTable } from '@/composables/useTable'
 
 export default {
   name: 'UserList',
-
-  mixins: [tableMixin, searchMixin, selectionMixin],
 
   props: {
     users: {
@@ -226,6 +224,13 @@
   },
 
   emits: ['user-edit', 'selection-changed'],
+  setup() {
+    const { rows, sortField, sortDirection, sortBy } = useTable()
+    const { searchQuery, searchResults, isSearching, search, clearSearch } = useSearch()
+    const { selectedItems, hasSelection, selectionCount, toggleSelection, selectAll, deselectAll, isSelected } = useSelection()
+
+    return { rows, sortField, sortDirection, sortBy, searchQuery, searchResults, isSearching, search, clearSearch, selectedItems, hasSelection, selectionCount, toggleSelection, selectAll, deselectAll, isSelected }
+  },
 
   data() {
     return {
```

## `src\components\users\UserProfile.vue`

```diff
--- a/src\components\users\UserProfile.vue
+++ b/src\components\users\UserProfile.vue
@@ -247,16 +247,21 @@
 </template>
 
 <script>
-import formMixin from '@/mixins/formMixin'
-import permissionMixin from '@/mixins/permissionMixin'
-import loadingMixin from '@/mixins/loadingMixin'
+import { useLoading } from '@/composables/useLoading'
+import { usePermission } from '@/composables/usePermission'
+import { useForm } from '@/composables/useForm'
 
 export default {
   name: 'UserProfile',
 
-  mixins: [formMixin, permissionMixin, loadingMixin],
-
   emits: ['form-submitted', 'profile-saved', 'profile-cancelled'],
+  setup() {
+    const { formData, isDirty, isSubmitting, formErrors, initForm, resetForm } = useForm()
+    const { canEdit, canDelete, permissionLevel } = usePermission()
+    const { isLoading, error, hasError, canRetry, startLoading, stopLoading, retry } = useLoading()
+
+    return { formData, isDirty, isSubmitting, formErrors, initForm, resetForm, canEdit, canDelete, permissionLevel, isLoading, error, hasError, canRetry, startLoading, stopLoading, retry }
+  },
 
   data() {
     return {
```

## `src\views\CalendarView.vue`

```diff
--- a/src\views\CalendarView.vue
+++ b/src\views\CalendarView.vue
@@ -95,15 +95,20 @@
 </template>
 
 <script>
+import { useLoading } from '@/composables/useLoading'
 import { useTasksStore } from '@/stores/tasks'
 import { formatDate } from '@/utils/helpers'
 import filterMixin from '@/mixins/filterMixin'
-import loadingMixin from '@/mixins/loadingMixin'
 
 export default {
   name: 'CalendarView',
 
-  mixins: [filterMixin, loadingMixin],
+  mixins: [filterMixin],
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+
+    return { isLoading, error, startLoading, stopLoading }
+  },
 
   data() {
     return {
```

## `src\views\DashboardView.vue`

```diff
--- a/src\views\DashboardView.vue
+++ b/src\views\DashboardView.vue
@@ -49,6 +49,7 @@
 </template>
 
 <script>
+import { useLoading } from '@/composables/useLoading'
 import StatsOverview from '@/components/dashboard/StatsOverview.vue'
 import RecentActivity from '@/components/dashboard/RecentActivity.vue'
 import ProjectSummaryChart from '@/components/dashboard/ProjectSummaryChart.vue'
@@ -56,7 +57,6 @@
 import UpcomingDeadlines from '@/components/dashboard/UpcomingDeadlines.vue'
 import { useProjectsStore } from '@/stores/projects'
 import { useTasksStore } from '@/stores/tasks'
-import loadingMixin from '@/mixins/loadingMixin'
 import chartMixin from '@/mixins/chartMixin'
 
 export default {
@@ -70,7 +70,12 @@
     UpcomingDeadlines
   },
 
-  mixins: [loadingMixin, chartMixin],
+  mixins: [chartMixin],
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+
+    return { isLoading, error, startLoading, stopLoading }
+  },
 
   data() {
     return {
```

## `src\views\LoginView.vue`

```diff
--- a/src\views\LoginView.vue
+++ b/src\views\LoginView.vue
@@ -118,15 +118,21 @@
 </template>
 
 <script>
+import { useForm } from '@/composables/useForm'
+import { useAuth } from '@/composables/useAuth'
 import { useAuthStore } from '@/stores/auth'
-import authMixin from '@/mixins/authMixin'
-import formMixin from '@/mixins/formMixin'
 import validationMixin from '@/mixins/validationMixin'
 
 export default {
   name: 'LoginView',
 
-  mixins: [authMixin, formMixin, validationMixin],
+  mixins: [validationMixin],
+  setup() {
+    const { login, checkAuth } = useAuth()
+    const { formData, initForm } = useForm()
+
+    return { login, checkAuth, formData, initForm }
+  },
 
   data() {
     return {
```

## `src\views\ProjectDetailView.vue`

```diff
--- a/src\views\ProjectDetailView.vue
+++ b/src\views\ProjectDetailView.vue
@@ -81,6 +81,7 @@
 </template>
 
 <script>
+import { useLoading } from '@/composables/useLoading'
 import ProjectBoard from '@/components/projects/ProjectBoard.vue'
 import ProjectMembers from '@/components/projects/ProjectMembers.vue'
 import ProjectSettings from '@/components/projects/ProjectSettings.vue'
@@ -88,7 +89,6 @@
 import TaskBoard from '@/components/tasks/TaskBoard.vue'
 import { useProjectsStore } from '@/stores/projects'
 import { useTasksStore } from '@/stores/tasks'
-import loadingMixin from '@/mixins/loadingMixin'
 import permissionMixin from '@/mixins/permissionMixin'
 
 export default {
@@ -102,7 +102,12 @@
     TaskBoard
   },
 
-  mixins: [loadingMixin, permissionMixin],
+  mixins: [permissionMixin],
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+
+    return { isLoading, error, startLoading, stopLoading }
+  },
 
   data() {
     return {
```

## `src\views\ProjectsView.vue`

```diff
--- a/src\views\ProjectsView.vue
+++ b/src\views\ProjectsView.vue
@@ -43,12 +43,12 @@
 </template>
 
 <script>
+import { useSearch } from '@/composables/useSearch'
+import { useLoading } from '@/composables/useLoading'
 import ProjectList from '@/components/projects/ProjectList.vue'
 import ProjectForm from '@/components/projects/ProjectForm.vue'
 import BaseModal from '@/components/common/BaseModal.vue'
 import { useProjectsStore } from '@/stores/projects'
-import loadingMixin from '@/mixins/loadingMixin'
-import searchMixin from '@/mixins/searchMixin'
 
 export default {
   name: 'ProjectsView',
@@ -58,8 +58,12 @@
     ProjectForm,
     BaseModal
   },
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+    const { searchQuery } = useSearch()
 
-  mixins: [loadingMixin, searchMixin],
+    return { isLoading, error, startLoading, stopLoading, searchQuery }
+  },
 
   data() {
     return {
```

## `src\views\ReportsView.vue`

```diff
--- a/src\views\ReportsView.vue
+++ b/src\views\ReportsView.vue
@@ -94,11 +94,11 @@
 </template>
 
 <script>
+import { useLoading } from '@/composables/useLoading'
 import ReportBuilder from '@/components/reports/ReportBuilder.vue'
 import ReportChart from '@/components/reports/ReportChart.vue'
 import ReportTable from '@/components/reports/ReportTable.vue'
 import ReportExport from '@/components/reports/ReportExport.vue'
-import loadingMixin from '@/mixins/loadingMixin'
 import chartMixin from '@/mixins/chartMixin'
 
 export default {
@@ -111,7 +111,12 @@
     ReportExport
   },
 
-  mixins: [loadingMixin, chartMixin],
+  mixins: [chartMixin],
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+
+    return { isLoading, error, startLoading, stopLoading }
+  },
 
   data() {
     return {
```

## `src\views\TaskDetailView.vue`

```diff
--- a/src\views\TaskDetailView.vue
+++ b/src\views\TaskDetailView.vue
@@ -119,10 +119,10 @@
 </template>
 
 <script>
+import { useLoading } from '@/composables/useLoading'
 import TaskDetail from '@/components/tasks/TaskDetail.vue'
 import TaskComments from '@/components/tasks/TaskComments.vue'
 import { useTasksStore } from '@/stores/tasks'
-import loadingMixin from '@/mixins/loadingMixin'
 import commentMixin from '@/mixins/commentMixin'
 
 export default {
@@ -133,7 +133,12 @@
     TaskComments
   },
 
-  mixins: [loadingMixin, commentMixin],
+  mixins: [commentMixin],
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+
+    return { isLoading, error, startLoading, stopLoading }
+  },
 
   data() {
     return {
```

## `src\views\TasksView.vue`

```diff
--- a/src\views\TasksView.vue
+++ b/src\views\TasksView.vue
@@ -74,6 +74,8 @@
 </template>
 
 <script>
+import { useFilter } from '@/composables/useFilter'
+import { useLoading } from '@/composables/useLoading'
 import TaskList from '@/components/tasks/TaskList.vue'
 import TaskBoard from '@/components/tasks/TaskBoard.vue'
 import TaskFilters from '@/components/tasks/TaskFilters.vue'
@@ -81,8 +83,6 @@
 import BaseModal from '@/components/common/BaseModal.vue'
 import { mapState, mapActions } from 'pinia'
 import { useTasksStore } from '@/stores/tasks'
-import loadingMixin from '@/mixins/loadingMixin'
-import filterMixin from '@/mixins/filterMixin'
 
 export default {
   name: 'TasksView',
@@ -94,8 +94,12 @@
     TaskForm,
     BaseModal
   },
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+    const { filters } = useFilter()
 
-  mixins: [loadingMixin, filterMixin],
+    return { isLoading, error, startLoading, stopLoading, filters }
+  },
 
   data() {
     return {
```

## `src\views\UsersView.vue`

```diff
--- a/src\views\UsersView.vue
+++ b/src\views\UsersView.vue
@@ -43,13 +43,13 @@
 </template>
 
 <script>
+import { useSearch } from '@/composables/useSearch'
+import { useLoading } from '@/composables/useLoading'
 import UserList from '@/components/users/UserList.vue'
 import UserForm from '@/components/users/UserForm.vue'
 import BaseModal from '@/components/common/BaseModal.vue'
 import { mapState, mapActions } from 'pinia'
 import { useUsersStore } from '@/stores/users'
-import loadingMixin from '@/mixins/loadingMixin'
-import searchMixin from '@/mixins/searchMixin'
 
 export default {
   name: 'UsersView',
@@ -59,8 +59,12 @@
     UserForm,
     BaseModal
   },
+  setup() {
+    const { isLoading, error, startLoading, stopLoading } = useLoading()
+    const { searchQuery } = useSearch()
 
-  mixins: [loadingMixin, searchMixin],
+    return { isLoading, error, startLoading, stopLoading, searchQuery }
+  },
 
   data() {
     return {
```

---

## Migration Checklist

### Blockers (must fix before code runs)

- [ ] 11 `this.$emit` → Accept an emit function parameter or use defineEmits
- [ ] 7 `this.$refs` → Use template refs with ref() instead
- [ ] 5 `this.$el` → Use a template ref on the root element instead
- [ ] 4 `external-dependency` → Accept 'formData' as a composable parameter and rewrite this.formData

### Manual Fixes (has known replacement)

- [ ] 2 `this.$router` → Import and use useRouter() from vue-router
- [ ] 1 `structural:nested-mixins` → Ensure all transitive mixin members are accounted for

### Advisory (review when convenient)

- [ ] 2 `this.$forceUpdate` → Reactive system usually handles it; review logic

### Summary

- **27** blocker(s) must be fixed before code will run
- **3** this.$ reference(s) need known replacements
